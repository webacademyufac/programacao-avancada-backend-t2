/* const vectorWork = [501, 4, 30, 205, 110 ]

const Objectchallenge = { 
    n1: vectorWork[0],
    n2: vectorWork[1],
    n3: vectorWork[2],
    n4: vectorWork[3],
    n5: vectorWork[4], 
}

const { n1, n2, n3, n4, n5 } = Objectchallenge 
console.log(JSON.stringify({n1,n2,n3,n4,n5}))
 */

/* const num1 = 10 
const num2 = 20 
const calc = { 
    soma: num1+num2, 
    sub: num1-num2, 
    mult: num1*num2, 
    div: num1/num2 
}
console.log(JSON.stringify(calc)) */ 

/* // estruturas, métodos de listas, funções 
const n = 100 
if(n > 10){ // > < >= <= == !=
    console.log('Teste de entrada no if.')
} 

const texto = '0'
if(texto==0) // quando tem === ele ver o igual e o tipo 
    console.log('Os dados são iguais.') 
    else if(true)
        console.log('Segundo if - if aninho')
    else 
        console.log('saída...') // erro na hora de pensar o código...
        //operador ternário 
    const resultado = n > 20 ? true : flase 
    console.log(resultado) 
    console.log(typeof resultado) 

 //repetição: precisa de 3 partes - variável de controle e a inicialização dela; condição/critério de parada; alteração do valor da variavel  de controle.  

    let contador = 0 
    const lista = [1, 7, 3, 64, 2, 0]
    while(contador < lista.length){ 
        console.log('0 \'elemento\' da\n vez é' + lista[contador] + '.')
        contador++ // contador = contador + 1  
    }

    const outraLista = ['a', 'b', 'd', 'e', 'c']
    for(let contador = 0; contador < outraLista.length; contador++) 
        console.log(`O elemento da vez é ${outraLista[contador]}.`) //template literais, ou template strings 
    const testeLiteral = (`\`==="`") */